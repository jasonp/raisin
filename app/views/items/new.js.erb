$("#add_todo_list_item_form-<%= escape_javascript(@list.id.to_s) %>").append("<%= escape_javascript(render 'items/new_item_form', :locals => {list: @list, project: @project, item: @item, assigness: @assignees}) %>");
$("#add_todo_item_link-<%= escape_javascript(@list.id.to_s) %>").empty();
$("body").trigger('loadDatePicker');