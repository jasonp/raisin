

<div class="row">
<div class="col-md-6 col-md-offset-3">

<h2>Forgot your password?</h2>

<p class="text-muted">No problem! We have a record of these things. We can't actually <em>read</em> your password, so we can't send it to you -- but we can send you a link that allows you to make a new one. 
	
<p class="text-muted">Just enter the email associated with your Raisin account below, and we'll send you an email with instructions for resetting your password.</p>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-group">
    <%= f.label :email, "Your email", class: "control-label" %>
		<div class="">
    <%= f.email_field :email, autofocus: true, class: "form-control" %>
		</div>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset password instructions", class: "btn btn-default" %>
  </div>
<% end %>

<div class="form-group nevermind_remebered_it">

	Or: Nevermind, <%= link_to "I remembered it!", new_user_session_path %>

</div>

</div>

</div>