
<nav class="navbar navbar-default">
	
	<div class="navbar-header">
	      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#raisin-navbar-collapse-1">
	        <span class="sr-only">Toggle navigation</span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	      </button>
	
	      <% unless @home_page -%>
					<a class="navbar-brand" href="<%= root_path %>"><%= image_tag 'raisin2.png' %></a>
				<% end -%>	
	    </div>
	
			<!-- Collect the nav links, forms, and other content for toggling -->
			<!-- If not logged in, there is no left menu! -->
			    <div class="collapse navbar-collapse" id="raisin-navbar-collapse-1">
			      
					<% if current_user %>
						<ul class="nav navbar-nav">			        
			        <li><%= link_to "Dashboard", root_path %></a></li>
							<li><%= link_to "My Tasks", user_path(current_user) %></li>
			      </ul>
					<% end %>
			
			
					<ul class="nav navbar-nav navbar-right">					
				
				<!-- If logged out -->
				<% if !current_user %>
					<li><a href="<%= beta_path %>">Sign up</a></li>
					<li><a href="<%= new_user_session_path %>">Sign in</a></li>
				<% else %>
				<!-- If logged in -->
					
						
						<% if unpaid_account = RaisinRoutines.show_payment_link_if_needed(session["preferred_account_id"], current_user) -%>
							<li><a href="<%= new_subscription_path %>" id="green_pay_link">Activate your account (
								<%= RaisinRoutines.time_until_account_expiration(unpaid_account) %>
								)</a></li>	
						<% end %>
						
						<li><%= link_to "Help", help_path %></li>
						
		        <li class="dropdown">
		          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= current_user.name || "Profile" %> <span class="caret"></span></a>
		          <ul class="dropdown-menu" role="menu">
		            <li><%= link_to "Edit my personal info", edit_user_path(current_user) %></a></li>
								<% if session["preferred_account_id"] != nil -%>
		            	<li><%= link_to "Edit my account info", edit_account_path(session["preferred_account_id"]) %></li>
									<li><%= link_to "Edit payment info", edit_subscription_path(1) %></li>
								<% end -%>
		            <li><%= link_to "Switch accounts", accounts_path %></li>
		            <li class="divider"></li>
		            <li><%= link_to('Log out', destroy_user_session_path, :method => :delete) %></li>
		          </ul>
		        </li>
				<% end %> <!-- if / else login -->
		      </ul>
		    </div><!-- /.navbar-collapse -->


</nav>