<%= form_for @project, url: account_project_path(@account, @project), method: :put , html: {class: "form new_project"} do |f| %>

<% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% @account.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>

	<div class="form-group">
	  <div class="controls"><%= f.text_field :title, autofocus: true, class: "form-control input-lg", placeholder: "Title The New Project" %></div>
	</div>

	<div class="form-group">
	  <div class="controls"><%= f.text_area :description, class: "form-control", placeholder: "(Optional) A short project description" %></div>
	</div>


  <div class="actions">
    <%= f.submit "Update the project", class: "btn" %>
  </div>
<% end %>